[package]
name = "diesel-guard"
version = "0.11.0"
edition = "2024"
authors = ["Alex Yarotsky"]
description = "Linter for dangerous Postgres migration patterns in Diesel and SQLx. Prevents downtime caused by unsafe schema changes."
documentation = "https://ayarotsky.github.io/diesel-guard/"
license = "MIT"
repository = "https://github.com/ayarotsky/diesel-guard"
homepage = "https://ayarotsky.github.io/diesel-guard/"
readme = "README.md"
keywords = ["linter", "orm", "postgresql", "sqlx", "diesel"]
categories = ["command-line-utilities", "development-tools"]

[dependencies]
# SQL parsing
pg_query = "6"

# CLI
clap = { version = "4.6", features = ["derive"] }
colored = "3.1"
human-panic = "2"

# Language Server Protocol
lsp-server = "0.7"
lsp-types = "0.97"

# Error handling
thiserror = "2.0"
libc = "0.2"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Config file
toml = "1.1"

# Scripting engine for custom checks
rhai = { version = "1", features = ["serde", "sync"] }

# Path handling
camino = "1.2"
walkdir = "2.4"

# Validation
regex = "1.12"

# Error reporting
miette = { version = "7.0", features = ["fancy-no-backtrace"] }

# Derive macros
derive_more = { version = "2.1", features = ["display", "from", "error"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = "symbols"
panic = "abort"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"

[dev-dependencies]
tempfile = "3.27"
assert_cmd = "2"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
pedantic = { level = "deny", priority = -1 }
# SQL keywords and identifiers in docs (NOT VALID, ACCESS EXCLUSIVE, etc.) look awkward in backticks
doc_markdown = "allow"
# Internal helpers and CLI binary; #[must_use] annotation on every helper adds noise without benefit
must_use_candidate = "allow"
# We don't maintain exhaustive API docs with # Errors sections
missing_errors_doc = "allow"
